# Buyer Trust Experience Implementation Plan

1. Add a focused Playwright contract for public landing content, images, navigation, and responsive overflow.
2. Make `/` a real public route without the authenticated commerce header.
3. Rebuild `LandingPage.tsx` around the buyer journey and seeded catalog imagery.
4. Replace landing CSS with a standalone responsive light/dark visual system.
5. Repair the highest-impact buyer proof/loading/dialog defects that can be verified in the remaining sprint.
6. Run the build and focused tests, then capture desktop and mobile screenshots under `output/playwright/buyer-ui-audit/`.
